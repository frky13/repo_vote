/*document.getElementById('connexion-form').addEventListener('submit', function(event) {
    var telephoneInput = document.getElementById('telephone');
    var mdpInput = document.getElementById('mdp');
    var telephoneError = document.getElementById('telephone-error');
    var mdpError = document.getElementById('mdp-error');
    
    var telephone = telephoneInput.value;
    var mdp = mdpInput.value;
    
    var telephoneExists = false;
    var validPassword = false;
    
    // Vérifier si le numéro de téléphone est enregistré
    // Ici, vous pouvez effectuer une requête AJAX ou accéder à votre base de données pour vérifier si le numéro de téléphone existe
    
    // Exemple de vérification statique (à remplacer par votre logique réelle
    
    // Vérifier si le mot de passe est correct
    if (mdp === "ace_f@mily2023") {
      validPassword = true;
    }
    
    // Afficher les messages d'erreur si nécessaire
    if (!telephoneExists) {
      telephoneError.textContent = "Numéro de téléphone non enregistré.";
      event.preventDefault(); // Empêche la soumission du formulaire
    } else {
      telephoneError.textContent = ""; // Réinitialise le message d'erreur
    }
    
    if (!validPassword) {
      mdpError.textContent = "Mot de passe incorrect.";
      event.preventDefault(); // Empêche la soumission du formulaire
    } else {
      mdpError.textContent = ""; // Réinitialise le message d'erreur
    }
  });
  */


   